## 脚本模式：

- 前端触发后，后端直接执行指定 playbook。
- 如果某个节点要求确认，就不会继续往下跑，而是生成 pending_confirmation，同时保存 resume_state。
- resume_state 里最关键的是已完成节点快照，比如 completed_nodes，以及恢复执行所需的状态。
- 用户确认后，不是“从内存里唤醒旧线程”，而是新开一个执行流程，把确认结果写回上下文，再把之前的 resume_state 传进去。
- playbook 重新进入执行器后，会读取快照，跳过已完成节点，只从待执行位置继续。

## 聊天模式：

- 前面多了一层 LLM/图流程，用来做意图判断、决定走哪个 playbook。
- 一旦进入脚本化 playbook，后面的确认、中断、恢复机制和脚本模式是同一套。
- 所以聊天的“确认逻辑”和脚本本身并没有两套实现，真正统一的是 playbook 层。