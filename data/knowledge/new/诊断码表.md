# 诊断码表

> 来源：`Sheet1.png` 图片内容整理。

## navigation

### ERROR

| code | message | condition | solution |
| --- | --- | --- | --- |
| 10000 | No global map input | 从未收到该消息 | 检查地图服务和全局地图消息是否正常：`/zj_humanoid/navigation/map` |
| 10001 | No local map input | 从未收到该消息 | 检查局部地图消息：`/zj_humanoid/navigation/local_map` |
| 10002 | No odometry input | 从未收到该消息 | 检查里程计消息：`/zj_humanoid/navigation/odom_info` |
| 10003 | Odometry timeout | 超过 2s 未收到该消息 | 检查里程计消息：`/zj_humanoid/navigation/odom_info` |
| 10004 | Local map timeout | 超过 2s 未收到该消息 | 检查局部地图消息：`/zj_humanoid/navigation/local_map` |
| 10005 | Invalid goal | 无效任务 | 1. 检查 waypoints 列表是否为空<br>2. 检查距离误差和角度误差是否不小于 0.0 |
| 10006 | Unsafe goal pose | 目标位姿不安全 | 检查目标点是否在空闲区域或离障碍物太近 |
| 10007 | Unsafe initial pose | 初始位姿不安全 | 1. 检查机器人当前位置是否在空闲区域或离障碍物太近<br>2. 检查定位是否正常 |
| 10008 | Arrival accuracy out of tolerance | 到点误差超限 | 1. 检查任务是否是正常结束<br>2. 检查目标点设置的容许误差是否合理 |
| 10009 | Planning Failed | 规划失败（同一位置连续多次） | 1. 确认当前环境下当前导航任务是否具有可行性<br>2. 检查机器人当前位置或目标点是否离障碍物太近。若是，修改全局规划的膨胀阈值<br>3. 检查是否机器人当前位置距目标点太远，导致搜索超时。若是，修改目标点位置或增大搜索时间 |

### WARN

| code | message | condition | solution |
| --- | --- | --- | --- |
| 20001 | Odometry delay | 超过 1s 未收到该消息 | 检查里程计消息：`/zj_humanoid/navigation/odom_info` |
| 20002 | Local map delay | 超过 1s 未收到该消息 | 检查局部地图消息：`/zj_humanoid/navigation/local_map` |
| 20003 | Blocked by obstacle | 被障碍物阻挡 | 检查机器人近处是否存在障碍物 |

### INFO

图片中 navigation 的 INFO 区域未填写内容。

## location

### ERROR

| code | message | condition | solution |
| --- | --- | --- | --- |
| 10000 | 1. LiDAR loop back: check if the LiDAR is working well<br>2. IMU loop back: check if the IMU is working well<br>3. No input data for 1s, check if the lidar and imu are working well<br>4. Too few input point cloud: check if the lidar is working well | 各种传感器数据异常：LiDAR、IMU 无数据，时间戳错乱，点云数据少 | 1. 检查是否存在点云、IMU 话题：`rostopic hz /livox/lidar` 或 `rostopic hz /livox/imu`<br>2. 检查点数是否在 20000 左右：`rostopic hz /livox/lidar \| grep "point_num"`<br>3. 重启传感器驱动 |
| 10001 | 1. Map directory does not exist<br>2. Map name does not exist<br>3. Global map does not exist<br>4. Failed to load global map<br>5. Session does not exist<br>6. Failed to open loc_result.txt | 配置错误，比如：<br>1. 地图路径不存在<br>2. 本次地图名不存在<br>3. 全局 pcd 点云不存在<br>4. 全局 PCD 点云加载失败<br>5. 当前 session 不存在<br>6. 无法写入 `loc_result.txt` | 检查地图路径，或换地图 |
| 10002 | Odometry lost: stop and restart | 里程计发散 | 重启里程定位服务 |
| 10003 | 1. Single Frame Computation TimeOut<br>2. Consecutive computation timeouts | 1. 单帧耗时超过 90ms，严重超时<br>2. 1s 内平均耗时超过 80ms | 检查当前负载情况 |
| 10004 | 1. BTC/KISS-MATCHER relocalization failed<br>2. Initpos relocalization failed | 1. 自动重定位失败<br>2. 初值重定位失败 | 图片中未填写 |

### WARN

| code | message | condition | solution |
| --- | --- | --- | --- |
| 20000 | 1. Reloc initializing, valid frame count<br>2. Lost localization for 5s: valid frame count | 定位未初始化；定位丢失 | 确保处于地图区域，并且可视环境良好，几何特征充分 |
| 20001 | 1. LiDAR sensor degrade: check if the LiDAR is blocked<br>2. Odometry degrade<br>3. Low confidence | 传感器退化（LiDAR 被遮住），里程计环境退化 | 离开退化区域；减少对 LiDAR 的遮挡 |
| 20002 | Odometry computation timeout | 单帧计算超时，超过 80ms | 检查当前负载情况 |
| 20003 | status not update | 无条件无需测试 | 图片中未填写 |

### INFO

| code | message | condition |
| --- | --- | --- |
| 1 |  | 初始化阶段，此时无定位 |
| 2 |  | 正常运行 |
| 3 |  | 各种异常 |
| 4 |  | 未初始化 |

## mapping

### ERROR

| code | message | condition | solution |
| --- | --- | --- | --- |
| 10000 | z_floor must be less than z_ceil | 建图参数错误 | 设置正确建图参数，`z_ceil > z_floor` |
| 10001 | 1. Too few points: check LiDAR<br>2. No sensor data: check LiDAR | 没有传感器数据；点数过少 | 1. 检查是否存在点云、IMU 话题：`rostopic hz /livox/lidar` 或 `rostopic hz /livox/imu`<br>2. 检查点数是否在 20000 左右：`rostopic hz /livox/lidar \| grep "point_num"`<br>3. 重启传感器驱动 |
| 10002 | 1. Mapping failed: check roslaunch command<br>2. Abort mapping<br>3. edge.txt not exists: check map_path<br>4. edge.txt is empty: no loop found<br>5. Load PCD failed: check map_path | 1. 建图 launch 文件启动错误<br>2. 中止建图<br>3. 没有 `edge.txt` 或 `edge.txt` 为空，表示没有生成回环<br>4. 加载 PCD 错误 | 1. 检查 `/datasets` 路径是否映射<br>2. 中止了后处理，可以重新建图<br>3. 扫描 30s 以上，并确保扫描区域产生回环<br>4. 检查是否生成 pcd 文件，文件大小等 |

### WARN

| code | message | condition | solution |
| --- | --- | --- | --- |
| 20001 | 1. Degeneration detected, reset system<br>2. Initialization failed, reset system | 动态初始化失败或退化，自动重置系统 | 延长采集时间 |

### INFO

| code | message | condition |
| --- | --- | --- |
| 1 |  | 无任务（未开始，或建图结束） |
| 2 |  | 采集中 |
| 3 |  | 后处理 |
| 4 |  | 异常 |

## perception

### ERROR

| code | message | condition | solution |
| --- | --- | --- | --- |
| 10000 | unrecognizable agent type | 设备类型错误 | 检查环境变量 `PERCEPTION_ROBOT_MODEL`，或者检查启动脚本中 `./ros_app_run xx`，给定或修改 `xx` 值以指定设备类型 |
| 10001 | no json config file found | json 配置文件地址错误 | 检查文件路径 |
| 10002 | no preference yaml file found | yaml 配置文件地址错误 | 检查文件路径 |
| 10003 | pcd num overflow | 点云数量超出设置值，一般为 realsense 分辨率设置错误 | 检查 realsense 配置中的分辨率是否为 640*480，或者更改 json 配置文件中的 `estimate_pts_num` 为更大数值后重新启动 |

### WARN

| code | message | condition | solution |
| --- | --- | --- | --- |
| 20000 | no valid points received from sensor | 传感器无点云 | 检查传感器是否正常，或检查传感器配置如 realsense 配置中 enable point cloud 是否打开 |
| 20001 | wait for transform | 等待传感器到本体位姿 | 等待或者检查静态是否正常，检查容器内是否能正常接收到位姿；上述均正常尝试重启，可能存在通信问题，需要多次重启 |
| 20002 | wait for transform | 等待里程计到本体位姿 | 等待或者检查静态是否正常，检查容器内是否能正常接收到位姿；上述均正常尝试重启，可能存在通信问题，需要多次重启 |
| 20003 | no valid points after filter | 范围过滤后无点云 | 检查 json 配置文件 `pts_msg_filter_args` 中的范围和距离配置是否合理；上述检查无异常时应考虑传感器安装位置及其安装矩阵是否正确配置于 `sensor_extra_trans_mat` |
| 20004 | no valid points after filter | 框过滤后无点云 | 检查 json 配置文件 `box_pts_filter_args` 中的范围和距离配置是否合理；上述检查无异常时应考虑传感器安装位置及其安装矩阵是否正确配置于 `sensor_extra_trans_mat` |
| 20005 | no valid voxels after map construction | 杂点滤波后无障碍物输出 | 检查 json 配置文件 `flatten_map_args` 中的范围和距离配置是否合理；上述检查无异常时应考虑传感器安装位置及其安装矩阵是否正确配置于 `sensor_extra_trans_mat` |
| 20006 | soft emergency stop in Cartesian mode, check localization & odom | 未建图点云经过滤后存在过近障碍 | 检查设备当前位置是否合理，检查 json 配置文件 `emergency_args` 中过滤框及范围距离等配置是否合理 |

### INFO

| code | message | condition |
| --- | --- | --- |
| 30000 | valid points num after range filter | 正常 |
