version: 1
description: robot not moving rules

rules:
  rule_1:
    type: compare
    field: output
    op: not_empty
    cast: string

  rule_2:
    type: compare
    field: output
    op: regex_match
    value: "(?m)^\\s*(?:x|y|z):\\s*(?!0(?:\\.0+)?(?:\\s|$))[-+]?(?:\\d+(?:\\.\\d+)?|\\.\\d+)"
